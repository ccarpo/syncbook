POSTGRES_USER=syncbook
POSTGRES_PASSWORD=syncbook
POSTGRES_DB=syncbook
POSTGRES_PORT=5432
SERVER_PORT=3000
WEB_PORT=8080
JWT_SECRET=change-me-in-production
APP_BASE_URL=http://localhost:8080
SMTP_URL=
MAIL_FROM=syncbook@localhost
# Host-side local development:
DATABASE_URL=postgres://syncbook:syncbook@localhost:5432/syncbook
# docker-compose supplies its own DATABASE_URL using the postgres service hostname.
